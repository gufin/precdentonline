# precedent-online-site

Веб-приложение для поиска юридических прецедентов с поддержкой семантического и классического поиска.

## Требования

- Node.js (версия 18 или выше)
- npm или yarn

## Установка

1. Клонируйте репозиторий или перейдите в директорию проекта:
```bash
cd precedent-online-site
```

2. Установите зависимости:
```bash
npm install
```

## Настройка окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```env
# API ключ для классического поиска (обязательно)
API_KEY=your_api_key_here

# Базовый URL API (опционально, по умолчанию https://api.you-right.ru/gas)
API_BASE=https://api.you-right.ru/gas

# Настройки семантического поиска
SEMANTIC_API_BASE=http://195.35.56.180
SEMANTIC_API_KEY=3a6fe05a871834862d13c3497a5df8c273e50e5d0aa67d8f0a7ef05a013ce93b

# Gemini API ключ (если используется)
GEMINI_API_KEY=your_gemini_key_here

# Порт для Express сервера (опционально, по умолчанию 3001)
PORT=3001
```

**Примечание:** Если файл `.env` отсутствует, приложение будет использовать значения по умолчанию из `server.js`.

## Запуск проекта

### Вариант 1: Запуск всех сервисов одновременно (рекомендуется)

Запускает и фронтенд (Vite), и бэкенд (Express) серверы одновременно:

```bash
npm run dev:all
```

Этот скрипт использует `concurrently` для параллельного запуска обоих серверов.

### Вариант 2: Запуск серверов отдельно

В одном терминале запустите Express сервер (бэкенд):
```bash
npm run dev:server
```

В другом терминале запустите Vite dev сервер (фронтенд):
```bash
npm run dev
```

## Доступ к приложению

После запуска приложение будет доступно по адресам:

- **Фронтенд:** http://localhost:3000
- **API сервер:** http://localhost:3001

Vite dev сервер автоматически проксирует запросы к `/api/*` на Express сервер на порту 3001.

## Сборка для продакшена

Для создания production сборки:

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`.

Для предпросмотра production сборки:

```bash
npm run preview
```

## Структура проекта

```
precedent-online-site/
├── components/          # React компоненты
│   ├── FilterPanel.tsx  # Панель фильтров
│   ├── Icons.tsx        # Иконки
│   ├── Modal.tsx        # Модальное окно
│   └── ResultCard.tsx   # Карточка результата
├── services/            # Сервисы
│   └── api.ts          # API клиент
├── App.tsx             # Главный компонент
├── index.tsx           # Точка входа
├── server.js           # Express сервер (бэкенд)
├── vite.config.ts      # Конфигурация Vite
└── package.json        # Зависимости и скрипты
```

## Функциональность

- **Семантический поиск** - поиск по смыслу запроса (требует запущенный сервис семантического поиска)
- **Классический поиск** - традиционный поиск по ключевым словам и номерам дел
- **Фильтрация** - фильтры по суду, судье, категории, дате и другим параметрам
- **Сортировка** - сортировка результатов по дате или суду
- **Пагинация** - постраничный вывод результатов

## Устранение неполадок

### Семантический поиск недоступен

Если вы видите ошибку "Семантический поиск недоступен":
1. Убедитесь, что сервис семантического поиска запущен и доступен по адресу, указанному в `SEMANTIC_API_BASE`
2. Проверьте правильность `SEMANTIC_API_KEY` в файле `.env`
3. Или переключитесь на классический поиск

### Ошибки API

Если возникают ошибки при запросах к API:
1. Проверьте наличие и правильность `API_KEY` в файле `.env`
2. Убедитесь, что API сервер доступен по адресу, указанному в `API_BASE`
3. Проверьте логи в консоли браузера и терминале сервера
