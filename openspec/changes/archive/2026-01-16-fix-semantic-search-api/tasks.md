# Implementation Tasks

## 1. Обновление конфигурации API
- [x] 1.1 Изменить константу `semanticApiBase` в `services/api.ts` с `http://195.35.56.180` на `https://semsearch.ru/`
- [x] 1.2 Обновить значение по умолчанию для `SEMANTIC_API_BASE` в `server.js` на `https://semsearch.ru`
- [x] 1.3 Обновить комментарии и документацию, указывающие на старый URL

## 2. Расширение типов для поддержки categories
- [x] 2.1 Добавить поле `categories?: string[] | null` в интерфейс `SearchFilters` в `types.ts`
- [x] 2.2 Добавить поле `categories?: string[] | null` в интерфейс `SearchResultItem` в `types.ts`
- [x] 2.3 Обновить функцию `adaptSearchResultToCaseRecord` для обработки поля `categories`

## 3. Тестирование
- [x] 3.1 Проверить подключение к `https://semsearch.ru/` и выполнение тестового запроса
- [x] 3.2 Протестировать семантический поиск без фильтров
- [x] 3.3 Протестировать семантический поиск с фильтрами `date_from`, `date_to`, `court`, `doctype`
- [x] 3.4 Убедиться, что ошибки подключения обрабатываются корректно

## 4. Документирование
- [x] 4.1 Добавить комментарии в код, указывающие доступные фильтры семантического поиска
- [x] 4.2 Обновить README.md с информацией о семантическом API
- [x] 4.3 Добавить примечание о различиях между фильтрами классического и семантического поиска
